import { countModel } from "./models/counter.model";
import { authModel } from "./models/auth.model";
import { appData } from "./models/app-data.model";
const models = {
  countModel,
  authModel,
  appData,
};

export default models;
